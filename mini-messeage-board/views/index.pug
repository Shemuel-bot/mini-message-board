extends layout

mixin list
  - for(let i = 0; i < messages.length; i++)
     h2 #{messages[i].user}
     p #{messages[i].text}
     p Date: #{messages[i].added}

block content
  h1= title
  +list
  a(href="/new")
    button New Comment

